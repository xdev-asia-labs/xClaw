name: Docker · Build & Publish Images

on:
  push:
    branches: [main]
    tags: ["v*"]
    paths:
      - "packages/*/src/**"
      - "packages/channels/*/src/**"
      - "Dockerfile"
      - "packages/web/Dockerfile"
      - ".github/workflows/docker-publish.yml"
  workflow_dispatch:
    inputs:
      tag:
        description: "Image tag override (e.g. v2.1.0)"
        required: false
        type: string

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  SERVER_IMAGE: ghcr.io/${{ github.repository_owner }}/xclaw-server
  WEB_IMAGE: ghcr.io/${{ github.repository_owner }}/xclaw-web

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: server
            dockerfile: Dockerfile
            context: .
            image_env: SERVER_IMAGE
          - name: web
            dockerfile: packages/web/Dockerfile
            context: .
            image_env: WEB_IMAGE
    steps:
      - uses: actions/checkout@v6

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine tags
        id: tags
        run: |
          IMAGE="${{ matrix.name == 'server' && env.SERVER_IMAGE || env.WEB_IMAGE }}"
          TAGS="${IMAGE}:sha-${GITHUB_SHA::7}"

          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
            TAGS="${TAGS},${IMAGE}:${VERSION},${IMAGE}:latest"
          elif [[ "${{ inputs.tag }}" != "" ]]; then
            TAGS="${TAGS},${IMAGE}:${{ inputs.tag }}"
          elif [[ "${{ github.ref_name }}" == "main" ]]; then
            TAGS="${TAGS},${IMAGE}:latest"
          elif [[ "${{ github.ref_name }}" == "develop" ]]; then
            TAGS="${TAGS},${IMAGE}:develop"
          fi

          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"

      - name: Build and push ${{ matrix.name }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,scope=${{ matrix.name }},mode=max

      - name: Trivy scan ${{ matrix.name }}
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ steps.tags.outputs.tags }}
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: true
